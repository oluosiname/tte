version: "3.8"

services:
  route-calculator:
    build: .
    volumes:
      - .:/app
    working_dir: /app

  test:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: bundle exec rspec
