services:
  route-calculator:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    entrypoint: ["ruby", "app.rb"]

  test:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: bundle exec rspec
